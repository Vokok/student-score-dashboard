services:
  - type: web
    name: student-score-dashboard
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install --prefer-binary -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
